[project]
name = "moshyn-ai"
version = "0.1.0"
description = "FastAPI backend service for moshyn-ai"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"