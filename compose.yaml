version: "3.9"

services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
    command: uvicorn moshyn.application.main:app --host 0.0.0.0 --port 8000 --reload
    restart: "no"
